import function
" Here the function module will change to bytecode and test.py change to bytecode both merge togather and store under pycache  later excute"
print(f"inside the test.py {__name__}")#main

#"output 
#add 
#function -here function.py is __name_ will consider has module name
#inside the test.py  __main__"
